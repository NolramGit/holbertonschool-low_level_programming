#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Description: 'Prints message whit puts function
 *
 * Return: Return 0
 */
int main(void)
{
	int ascii = 97;

	for (; ascii <= 122; ascii++)
	{
		if (ascci != 101 && ascii != 113)
			putchar(ascii);
	}
	putchar(10);
	return (0);
}
