#include "main.h"
#include <string.h>
/**
 * _puts - Entry function
 *
 * Description: Function that returns length of two a string.
 *
 * @s: pointer.
 *
 * Return: Always 0.
 */

void _puts(char *s)
{
	puts(s);
}
