#include "main.h"
#include <string.h>
/**
 * _strlen - Entry function
 *
 * Description: Function that returns length of two a string.
 *
 * @s: pointer.
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
